# price-checking-app-backend
Price checking app that saves items in a wishlist.

Download repository and run the following command to create a .zip to upload to related AWS Lambda function:

    Compress-Archive -Path * -DestinationPath ..\backend-lambda.zip -Force
